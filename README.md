# Public IP Home Assistant

Simple Home Assistant integration that exposes your public IP address as a sensor.

## Features

- UI configuration
- No YAML required
- Automatic hourly updates
- Multiple fallback providers
- HACS compatible

## Entity

- sensor.public_ip
